PORT=4000
# Local dev default points at the Docker container from docker-compose.yml.
# To move to a hosted Postgres (Supabase/Neon/RDS), replace this one line —
# then run `npm run migrate` against the new host.
DATABASE_URL=postgres://postgres:saa_dev_pass@localhost:5432/student_academic
JWT_SECRET=dev-only-secret-change-me
